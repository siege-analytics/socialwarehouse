## Self-Review: v2.3.0 promote develop → main

### Role declarations
- Author: AI agent
- Reviewer: AI agent (self-review)

Working as: software engineer

## Assumptions

Goal source: User directive "Yes" to proceed with v2.3.0 promotion after develop/main sync completed. Ontology epic #284 (A-0 through A-9) all merged and closed. Release event per develop-guard skill.

- develop is a proper superset of main (25 commits ahead, 0 behind) after sync merge
- Version bumped from 2.2.1-dev to 2.3.0 in pyproject.toml and socialwarehouse/__init__.py
- All 511 tests passing on CI (per A-9 PR #304)
- No breaking changes to existing APIs — all ontology models and endpoints are additive
- Downstream consumers are aware via cross-agent message

## Peer review

writing-code:1 — version string change only (2 files). No model, view, or logic changes in the promotion commit. Verified by `git diff --name-only`.

writing-claims:1 — release notes describe only what was actually implemented and merged (ontology epic #284, orchestration #275/#283, civic_lookup #272). No aspirational features.

## Lead review

- [x] All sub-issues of #284 are closed (verified via `gh issue view` on #284-#294)
- [x] develop ⊇ main (sync merge pushed, 0 commits on main not in develop)
- [x] Version bump is the only change in the promotion commit
- [x] promote/* branch naming follows established convention (promote/develop-to-main-v2.2.0 was the prior release)
