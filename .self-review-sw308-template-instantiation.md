# Self-Review: SW#308 Template Instantiation CLI Commands

## Assumptions
Domain(s): software engineering
Geospatial cross-cut: no
Goal source: ticket #308
Goal source verification: manual — ticket has Context, Goal, Acceptance criteria, Assumptions sections
Plan reference: sessions/260601-tall-swan/plans/sw308-design-note.md (posted to #308)
Pre-author-inventory: NONE
Trivial-against-state: no runtime-state contact points — these are new CLI commands with no existing entity modification
Investigate-artifact: TRIVIAL
Pre-mortem-artifact: TRIVIAL

## Trivial-investigation declaration

Category: new-additive-feature
Cannot produce error: all four commands are new additions to `swh/cli.py` with no modification to existing command signatures or behavior; no existing imports, models, or data shapes are changed.
Evidence: `git diff --stat` shows only additions to `swh/cli.py` (214 insertions, 0 deletions to existing lines beyond the import addition), plus two new files (`swh/template.py`, `swh/doctor.py`) and doc updates.
Falsification: an existing `swh` CLI command breaks or changes behavior after this change.

## Peer review

### writing-code
- No speculative abstractions: each new function serves a specific CLI command.
- Symbols verified: `generate_env` and `find_repo_root` are defined in `swh/template.py` and imported in `cli.py`; `Status`, `run_all_checks` defined in `swh/doctor.py` and imported in `cli.py`.
- Doc-edit symmetry: CLAUDE.md updated with new Template CLI Commands section; `docs/quickstart.md` updated with `swh init` quick path and `swh seed` equivalences.
- No silent processes: `swh init` prints what it wrote; `swh seed` echoes the command before running; `swh upgrade` echoes each step; `swh doctor` prints results table.

### writing-tests
- `test_template_init.py`: tests .env generation (required vars, unique SECRET_KEY, default/custom db_user, redis_url, states), CLI invocation (writes .env, skips existing, shows next steps).
- `test_doctor.py`: tests individual checks (python version pass/fail, env file missing, disk space, spark warn), CLI invocation (runs, JSON format).
- Tests import the modules they test (`from swh.cli import cli`, `from swh.template import generate_env`, `from swh.doctor import ...`).

### writing-claims
- No completeness claims made.
- File count: 5 files modified/added for the implementation (cli.py, template.py, doctor.py, test_template_init.py, test_doctor.py) plus 2 doc files (CLAUDE.md, quickstart.md).

### writing-prose
- No AI-typographic Unicode in any output.
- No structured "Why: / How to apply:" blocks in code comments.

## Lead review

In software engineering: the four commands are additive, self-contained, and follow existing CLI patterns (Click decorators, lazy imports, subprocess delegation for Django commands). No existing command signatures changed.

Approach-fit: Option A (thin CLI wrappers) matches the existing `swh/cli.py` architecture where each command is a Click function with lazy imports. `init` and `doctor` work without Django; `seed` and `upgrade` shell out to `manage.py`.

Blast radius: additive only. The only modified existing file is `swh/cli.py` (new import `os`, new commands appended before `__main__`). Documentation updates are additive sections.

Sequencing assumption: no upstream dependencies. This is a leaf ticket.

Junior dismissals reviewed: none flagged.

Knowledge loci updated: CLAUDE.md (Template CLI Commands section added), docs/quickstart.md (quick path added, seed section updated).

## Quantified claims

- "5 files modified/added" — `git diff --stat` + `git status --short | grep -E '^\?\? swh/(template|doctor)\.py|tests/unit/swh/test_(template_init|doctor)\.py'` → swh/cli.py, swh/template.py, swh/doctor.py, tests/unit/swh/test_template_init.py, tests/unit/swh/test_doctor.py
- "214 insertions" to cli.py — `git diff --stat swh/cli.py` → "214 +++"
