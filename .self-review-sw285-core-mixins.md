# Self-Review: feat(core) A-0 / #285 — core app with entity resolution infrastructure

## Assumptions
Working as: software engineer, data engineer
Peer review needed from: software engineer
Lead review needed from: data engineer, tech lead
Goal source: siege-analytics/socialwarehouse#285 (A-0: Core Infrastructure Mixins)
Goal source verification: evaluate-ticket.sh not yet deployed; ticket #285 exists and defines scope (core app, abstract mixins, UUID generation, EntityIdentifier model, Delta schema).
Plan reference: plans/think-sw285-core-mixins.md (session design note)

## Peer review (mechanics, correctness, craft floor)

### writing-code:1, writing-code:3, writing-code:7, writing-code:15
- writing-code:1 (no speculative abstractions): `git diff HEAD~1..HEAD -- '*.py' | grep -c 'TODO\|FIXME\|placeholder\|stub'` returns 0. All code paths are complete; no stubs or future-work markers.
- writing-code:3 (verify symbols exist): `SW_NAMESPACE`, `NORMALIZER_VERSION`, `normalize_v1`, `generate_entity_uuid5`, `generate_entity_uuid4` all defined in `mixins.py` and imported by `test_mixins.py`. `EntityIdentifier` defined in `models.py` and imported by `test_models.py`.
- writing-code:7 (no silent processes): All functions have explicit return values. `register()` returns `(obj, created)` tuple. `resolve()` returns queryset.
- writing-code:15 (sqlite timeout): `grep -rn 'sqlite3.connect' tests/` returns no matches. No sqlite3.connect calls in this diff.
- Unused imports removed: Removed `import pytest` from `test_mixins.py` (caught during self-review; no `@pytest.mark` used in that file).

### writing-tests:1, writing-tests:2
- writing-tests:1 (tests must fail on revert): UUID5 determinism test asserts `a == b` for same inputs; removing `normalize_v1` or changing `SW_NAMESPACE` would break. EntityIdentifier tests assert `created is True` on first call, `created is False` on second; removing `get_or_create` would break idempotency test. `resolve_excludes_expired` sets `valid_to` and asserts count 0; removing the `valid_to__isnull=True` filter would break.
- writing-tests:2 (tests import module under test): `test_mixins.py` imports from `socialwarehouse.core.mixins`; `test_models.py` imports from `socialwarehouse.core.models`.
- All tests verified passing: Mixin tests via inline script (all PASS). Model tests via sqlite-backed Django setup (all 7 PASS: register_creates, register_idempotent, register_jurisdiction, resolve_current, resolve_by_source, resolve_excludes_expired, resolve_no_match).

### writing-claims:1, writing-claims:2
- writing-claims:1 (grep before declaring complete): `TABLES['silver.entity_identifiers']` verified present via Python import with `SW_WAREHOUSE_ROOT=file:///tmp/sw-warehouse`. Fields: `['entity_uuid', 'identifier_type', 'identifier_value', 'data_source', 'jurisdiction_level', 'jurisdiction_state', 'normalizer_version', 'valid_from', 'valid_to', 'ingested_at']`.
- writing-claims:2 (countable claims grounded): Migration compiles: `importlib.util.spec_from_file_location` load succeeded; `Migration.initial is True`; 4 operations (CreateModel + 3 AddIndex). INSTALLED_APPS updated: `socialwarehouse.core` added before `socialwarehouse.geo` in `settings/base.py` line 55.

### writing-prose:1
- writing-prose:1 (no AI-typographic unicode): No smart quotes, em-dashes, or en-dashes in any changed files. Docstrings use plain ASCII throughout.

## Lead review (approach-fit, blast radius, sequencing)

As data engineer: the Delta schema for `silver.entity_identifiers` mirrors the Django model field-for-field. `entity_uuid` is StringType (not binary) for Spark/Parquet interop. Partitioned by `data_source` which is the primary access pattern for cross-source resolution. Schema is append-only by convention (no deletes; superseded rows get `valid_to` set). Standard not shelved; applied per usual append-only-log practice for entity resolution.

As data engineer: UUID5 generation is deterministic and reproducible — `uuid.uuid5(SW_NAMESPACE, normalize_v1(*components))`. Same inputs always yield same UUID. Normalizer is versioned (`NORMALIZER_VERSION = 1`) so future normalization changes don't break existing UUIDs. Standard not shelved; applied per usual deterministic-key-generation practice.

As tech lead: blast radius is minimal. New `socialwarehouse.core` app is entirely additive — no existing models modified, no existing migrations affected, no existing tests changed. The only existing files touched are `settings/base.py` (+1 line) and `delta/tables.py` (+23 lines). Core app must precede other SW apps in INSTALLED_APPS since A-1+ will inherit from its abstract models.

As tech lead: sequencing is correct. A-0 is the foundation; A-1 (DimPerson evolution) depends on `SourceAwareModel`, `IdentifiableModel`, and `EntityIdentifier` being available. No downstream tickets can proceed without this landing first.

## Evidence-predates-work
Artifact: .self-review-sw285-core-mixins.md
First-added commit: artifact written after work commit (review artifact by nature)
Work commit: b95b9a182eb4164fc2e9de5011d09baa9de50f6d
Verification: artifact will be added in the amend commit that adds the Self-Review trailer
