## Self-Review: Sync main → develop (v2.2.0 release commits)

### Role declarations
- Author: AI agent
- Reviewer: AI agent (self-review)

Working as: software engineer

## Assumptions

Goal source: Memory `feedback_repair_develop_default` — "default behavior when develop is behind main is sync-then-promote, not skip-the-choreography." develop was 2 commits behind main (v2.2.0 release commit + promote merge).
- The 2 commits on main but not develop are release ceremony only (tag + promote merge), no code divergence
- Version conflict resolution: kept develop's `2.2.1-dev` over main's `2.2.0` because develop is the active line
- After merge, develop is a proper superset of main (24 commits ahead, 0 behind)

## Peer review

writing-code:1 — only 2 files touched (pyproject.toml, __init__.py), both version strings. Verified by `git diff --name-only`.

writing-code:13 — no new I/O, no new imports, no behavioral change. Pure version-string conflict resolution.

## Lead review

- [x] develop is now superset of main (`git log origin/main..HEAD` shows 24 commits, `git log HEAD..origin/main` shows 0)
- [x] Version string kept as `2.2.1-dev` (develop's value, not main's `2.2.0`)
- [x] No code changes beyond conflict resolution markers
