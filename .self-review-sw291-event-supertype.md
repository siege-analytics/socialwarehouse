# Self-Review: SW#291 — Event Supertype (A-6)

## Assumptions
Working as: software engineer, data engineer
Peer review needed from: software engineer
Lead review needed from: tech lead, data engineer
Goal source: #291
Plan reference: Epic A General Civic Ontology — ticket A-6 Event Supertype

## Peer review (mechanics, correctness, craft floor)

### writing-code
- No raw SQL — Django ORM only
- No FKs across app boundaries — agent linkage by UUID (contest_uuid, relationship_uuid stored as UUIDField, not FK)
- OneToOne detail tables (CorporateEvent, SpatioTemporalEvent, ElectoralEvent) cascade from Event
- EventParticipant bridge enables shared query surface without UNIONs
- `save()` auto-derives `year` from `event_date` — same pattern as Election in A-4
- Migration hand-written (GDAL constraint); field types/defaults verified against model declarations
- Delta schemas added for all five tables; partition columns match query patterns
- writing-code:15 — no untimed I/O calls in this diff (no sqlite3.connect, no raw file opens)

### writing-tests
- 7 test classes covering all 5 models + shared query surface + edge cases
- `TestSharedQuerySurface` creates 3 events (transaction, corporate, electoral) linking 2 agents, verifies cross-type query returns correct counts
- Unique constraint tested: same (event, agent_uuid, role_in_event) triple raises Exception
- Same agent with different roles on same event passes — 2 participants created, count verified
- `__str__` methods tested for Event and ElectoralEvent (including certified suffix)
- Tests import the modules they test (from socialwarehouse.events.models import ...)

### writing-claims
- Event model inherits SourceAwareModel — jurisdiction_level/state/data_source come from mixin, not redeclared on Event
- `event_uuid` uses UUID4 (generate_entity_uuid4) — events are artifacts, not identity entities; consistent with A-5 transaction_uuid pattern
- `relationship_uuid` on CorporateEvent references A-3 RelationshipCorporateSuccession by UUID (not FK)
- `contest_uuid` on ElectoralEvent references A-4 ElectoralContest by UUID (not FK)
- Index names follow project convention: idx_event_*, idx_evpart_*, idx_evcorp_*, idx_evst_*, idx_evel_*
- Migration has 5 CreateModel + 11 AddIndex = 16 operations; count verified by reading the migration file

### writing-prose
- No AI-typographic Unicode in any file
- No structured "Why:" / "How to apply:" blocks in code comments or commit messages

## Lead review (approach-fit, blast radius, sequencing)

As software engineer: Event supertype with detail-table pattern is the standard Django approach for type hierarchies where subtypes have different fields. OneToOne to Event + CASCADE gives clean relational integrity without multi-table inheritance overhead.

As data engineer: Delta schemas mirror the Django model structure. Partition strategies align with expected query patterns — events by (event_type, jurisdiction_state, year); participants by event_type; spatiotemporal by affected_jurisdiction_state. No partition on corporate/electoral detail tables (low cardinality, small expected volume relative to events).

As tech lead: blast radius is contained to the new `socialwarehouse.events` app. Only shared files touched are `settings/base.py` (INSTALLED_APPS addition) and `delta/tables.py` (new schemas appended). No existing model modified. Sequencing: A-6 depends on A-0 (core mixins) which is merged; references A-3 (relationship_uuid) and A-4 (contest_uuid) by UUID, so no migration dependency.

## Files touched
- `socialwarehouse/events/__init__.py` — app config
- `socialwarehouse/events/apps.py` — EventsConfig (label=sw_events)
- `socialwarehouse/events/models/events.py` — 5 models
- `socialwarehouse/events/models/__init__.py` — public exports
- `socialwarehouse/events/migrations/0001_initial.py` — 5 CreateModel + 11 AddIndex
- `socialwarehouse/delta/tables.py` — 5 Silver schemas + TABLES entries
- `socialwarehouse/settings/base.py` — INSTALLED_APPS
- `tests/unit/events/test_events.py` — 7 test classes
