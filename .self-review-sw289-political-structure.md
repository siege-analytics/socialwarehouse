# Self-Review: feat(political) A-4 / #289 — Political structure models

## Assumptions
Working as: software engineer, data engineer
Peer review needed from: software engineer
Lead review needed from: data engineer, tech lead
Goal source: siege-analytics/socialwarehouse#289 (A-4: Political structure — Election, Office, Seat, OfficeTerm)
Goal source verification: evaluate-ticket.sh not yet deployed; ticket #289 exists and defines Delta-first schemas, Django models, and acceptance criteria.
Plan reference: ticket #289 acceptance criteria

## Peer review (mechanics, correctness, craft floor)

### writing-code:1, writing-code:3, writing-code:7
- writing-code:1 (no speculative abstractions): Five models exactly as specified. Office reuses SourceAwareModel's jurisdiction_level/jurisdiction_state rather than overriding with choices — same field, no duplication. CongressionalTerm is nullable columns on OfficeTerm rather than separate model — ticket explicitly says "generic + CongressionalTerm specialization."
- writing-code:3 (verify symbols exist): `SourceAwareModel`, `IdentifiableModel`, `generate_entity_uuid4`, `generate_entity_uuid5` all imported from `socialwarehouse.core.mixins` — verified importable. All 5 models exported from `socialwarehouse.political.models.__init__`. `sw_political.ElectoralContest` referenced by string in FactElectionResult/FactPrecinctResult FK — verified resolves at Django setup time.
- writing-code:7 (no silent processes): `Election.save()` derives `year` from `election_date` — explicit, tested. `is_current` properties on Seat and OfficeTerm are explicit boolean returns.

### writing-tests:1, writing-tests:2
- writing-tests:1 (tests fail on revert): `test_office_persists_across_redistricting` creates 2 seats for 1 office — removing the Office FK would break it. `test_temporal_query_who_held_office_on_date` exercises `start_date`/`end_date` filtering — removing those fields breaks the query. `test_full_chain_election_to_term` exercises the complete chain (election -> contest -> office -> term) — removing any FK breaks it.
- writing-tests:2 (tests import module under test): All test classes import from `socialwarehouse.political.models`. `generate_entity_uuid5` imported from `socialwarehouse.core.mixins` for deterministic Office UUIDs.

### writing-claims:1, writing-claims:2
- writing-claims:1 (grep before declaring complete): 5 models verified via sqlite `objects.create()`. Migration has 5 CreateModel + 10 AddIndex = 15 operations. 5 Delta schemas registered in TABLES dict. Nullable `electoral_contest` FK added to FactElectionResult and FactPrecinctResult with separate migration (0005).
- writing-claims:2 (countable claims grounded): 15 migration operations in 0001_initial. 4 operations in warehouse 0005. 25 test methods across 6 test classes. All verified via grep/read.

### writing-prose:1
- writing-prose:1 (no AI-typographic unicode): No smart quotes or special characters in changed files.

## Lead review (approach-fit, blast radius, sequencing)

As data engineer: Office merge key is (jurisdiction_level, jurisdiction_state, chamber, district_number) — does NOT include plan ID, so offices persist across redistricting as ticket requires. Seat is plan-specific with FK to Office. Election partitioned by (year, jurisdiction_state). OfficeTerm handles generic (term_type) + congressional specialization (congress_number nullable) without separate model.

As tech lead: blast radius is mostly additive. New `socialwarehouse.political` app with 5 tables. The one modification to existing code: nullable FK `electoral_contest` added to FactElectionResult/FactPrecinctResult — nullable so existing data is unaffected. Separate migration (0005) for the FK to keep it reviewable. INSTALLED_APPS updated to include `socialwarehouse.political`.

As tech lead: sequencing correct. A-4 depends on A-2/A-3 (candidates are Persons with Roles). A-5 (transactions) and A-6 (events) are parallel-ready after this.

## Evidence-predates-work
Artifact: .self-review-sw289-political-structure.md
First-added commit: artifact included in the same commit
Work commit: (determined at commit time)
