# Self-Review: feat(warehouse) A-1 / #286 — evolve DimPerson into ontology-compatible Person

## Assumptions
Working as: software engineer, data engineer
Peer review needed from: software engineer
Lead review needed from: data engineer, tech lead
Goal source: siege-analytics/socialwarehouse#286 (A-1: Agent hierarchy — evolve DimPerson)
Goal source verification: evaluate-ticket.sh not yet deployed; ticket #286 exists and defines Delta-first schema extension, nullable migration, backfill, and named materializer test.
Plan reference: ticket #286 acceptance criteria

## Peer review (mechanics, correctness, craft floor)

### writing-code:1, writing-code:3, writing-code:7
- writing-code:1 (no speculative abstractions): All code paths complete. DimPerson inherits both mixins, materializer maps all 6 new fields, migration adds columns and backfills. No stubs.
- writing-code:3 (verify symbols exist): `SourceAwareModel` and `IdentifiableModel` imported from `socialwarehouse.core.mixins` in dimensions.py. `generate_entity_uuid5` imported in materialize.py. All verified present in mixins.py from A-0.
- writing-code:7 (no silent processes): Materializer kwargs function explicitly maps all new fields. Backfill migration prints nothing but updates rows in batches. Reverse migration provided.

### writing-tests:1, writing-tests:2
- writing-tests:1 (tests fail on revert): `test_ontology_mixin_fields_populated` asserts `data_source == 'ts'` and `entity_uuid == generate_entity_uuid5('ts', 'TS001')` — removing the materializer mapping lines would fail this. `test_ts_materializer_writes_after_migration` is the ticket's named test; it queries a materialized DimPerson and asserts all 5 mixin fields populated.
- writing-tests:2 (tests import module under test): Test imports `from swh.voters.materialize import _silver_persons_to_dimperson_kwargs` and `from socialwarehouse.core.mixins import generate_entity_uuid5`.

### writing-claims:1, writing-claims:2
- writing-claims:1 (grep before declaring complete): Migration compiles with 8 operations (6 AddField + 1 AddIndex + 1 RunPython). Delta schema update verified: `entity_uuid`, `data_source`, `jurisdiction_level`, `source_record_id` all present in SILVER_PERSONS. Materializer kwargs verified: `data_source`, `jurisdiction_level`, `jurisdiction_state`, `source_record_id`, `ingested_at`, `entity_uuid` all mapped.
- writing-claims:2 (countable claims grounded): 6 new tests added to test_materialize.py. Migration has 8 operations. `_DIMPERSON_UPDATE_FIELDS` extended with 6 new field names.

### writing-prose:1
- writing-prose:1 (no AI-typographic unicode): No smart quotes or special characters in changed files.

## Lead review (approach-fit, blast radius, sequencing)

As data engineer: entity_uuid generated deterministically from `(vendor, vendor_voter_id)` via `generate_entity_uuid5`. Same natural key = same UUID across materializer runs and backfill. Backfill uses batched `bulk_update` (5000 rows/batch) to avoid OOM on large tables. Standard not shelved; applied per usual deterministic-key-generation practice.

As data engineer: Delta schema extension is additive — 4 new nullable columns added after `vendor_extras`, before `source_file`. No existing column changes type or position. Partition strategy unchanged. Standard not shelved; applied per usual additive-schema-evolution practice.

As data engineer: backfill maps vendor short codes to canonical source names (`ts` -> `targetsmart`). `jurisdiction_level='state'` is correct because voter files are state-level products. `jurisdiction_state` derived from `registration_state` (vendor-provided). Standard not shelved; applied per ticket's Step 2 specification.

As tech lead: blast radius is controlled. DimPerson gains 6 nullable columns — no existing column changed, no existing query broken. entity_uuid is nullable during transition (unique constraint still applied where non-null). Materializer maps new fields alongside existing ones; `_DIMPERSON_UPDATE_FIELDS` list extended. Existing end-to-end tests (`test_materialize_all_from_ts_fixtures`, `test_materialize_is_idempotent`) still pass unchanged — they don't assert absence of new fields.

As tech lead: sequencing correct. A-1 depends on A-0 (core mixins). Migration declares dependency on `sw_core:0001_initial`. A-2 (Committee + Organization) and A-3 (facets) will follow once this lands.

## Evidence-predates-work
Artifact: .self-review-sw286-dimperson-evolution.md
First-added commit: artifact written alongside work commit
Work commit: (to be determined at commit time)
Verification: artifact included in the same commit
