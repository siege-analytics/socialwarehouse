# Self-Review: feat(agents) A-2 / #287 — Committee and Organization models

## Assumptions
Working as: software engineer, data engineer
Peer review needed from: software engineer
Lead review needed from: data engineer, tech lead
Goal source: siege-analytics/socialwarehouse#287 (A-2: Agent hierarchy — Committee and Organization models)
Goal source verification: evaluate-ticket.sh not yet deployed; ticket #287 exists and defines Delta-first schemas, Django models, and acceptance criteria.
Plan reference: ticket #287 acceptance criteria

## Peer review (mechanics, correctness, craft floor)

### writing-code:1, writing-code:3, writing-code:7
- writing-code:1 (no speculative abstractions): Both models are complete with all fields from the ticket. No stubs, placeholders, or incomplete code paths.
- writing-code:3 (verify symbols exist): `SourceAwareModel`, `IdentifiableModel` imported from `socialwarehouse.core.mixins` in both model files. Verified both classes exist and are importable. `Committee` and `Organization` exported from `socialwarehouse.agents.models.__init__`.
- writing-code:7 (no silent processes): `Committee.is_active` property returns explicit boolean. `Organization.__str__` conditionally includes industry code. No silent side effects.

### writing-tests:1, writing-tests:2
- writing-tests:1 (tests fail on revert): `test_create_active_committee` asserts `is_active is True` — removing `termination_date` field or changing `is_active` logic would fail. `test_natural_key_uniqueness` exercises the `unique_together` constraint — removing it would allow duplicates. `test_entity_uuid_unique` exercises the `unique=True` on entity_uuid.
- writing-tests:2 (tests import module under test): `test_committee.py` imports `Committee` from `socialwarehouse.agents.models`. `test_organization.py` imports `Organization` from `socialwarehouse.agents.models`. Both import `generate_entity_uuid5` from `socialwarehouse.core.mixins`.

### writing-claims:1, writing-claims:2
- writing-claims:1 (grep before declaring complete): Both models verified via Python import and `_meta.get_fields()`. Committee has: entity_uuid, data_source, jurisdiction_level, jurisdiction_state, source_record_id, ingested_at, name, committee_type, source_system_id, formation_date, termination_date, name_history. Organization has: entity_uuid, data_source, name, industry_code, industry_system. Delta schemas registered: `silver.committees` (11 fields, partitioned by jurisdiction_level/jurisdiction_state), `silver.organizations` (9 fields, partitioned by jurisdiction_state).
- writing-claims:2 (countable claims grounded): Migration has 6 operations (2 CreateModel + 4 AddIndex). 5 committee tests + 5 organization tests. All pass against sqlite.

### writing-prose:1
- writing-prose:1 (no AI-typographic unicode): No smart quotes or special characters in changed files.

## Lead review (approach-fit, blast radius, sequencing)

As data engineer: Committee Delta schema partitioned by `(jurisdiction_level, jurisdiction_state)` for state-scoped queries. Merge key is `(data_source, source_system_id)` using registration ID, not name — handles name changes correctly. Organization partitioned by `jurisdiction_state` only since volume is lower. Both schemas follow the same column naming convention as silver.persons.

As tech lead: blast radius is minimal. New `socialwarehouse.agents` app is entirely additive — no existing models modified. INSTALLED_APPS updated to include `socialwarehouse.agents` between `socialwarehouse.core` and `socialwarehouse.geo`. Migration depends only on `sw_core:0001_initial`.

As tech lead: sequencing correct. A-2 depends on A-0 (core mixins, now merged). A-3 (Classification, Role, Relationship facets) will link these agent models together.

## Evidence-predates-work
Artifact: .self-review-sw287-committee-organization.md
First-added commit: artifact included in the same commit
Work commit: (determined at commit time)
Verification: artifact included in the same commit
