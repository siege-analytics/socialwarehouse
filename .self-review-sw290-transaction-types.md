# Self-Review: feat(transactions) A-5 / #290 — Transaction types

## Assumptions
Working as: software engineer, data engineer
Peer review needed from: software engineer
Lead review needed from: data engineer, tech lead
Goal source: siege-analytics/socialwarehouse#290 (A-5: Transaction types — Contribution, Expenditure, Transfer, Obligation)
Goal source verification: evaluate-ticket.sh not yet deployed; ticket #290 exists.
Plan reference: ticket #290 acceptance criteria

## Peer review (mechanics, correctness, craft floor)

### writing-code:1, writing-code:3, writing-code:7
- writing-code:1 (no speculative abstractions): `_TransactionBase` abstract justified by 4 concrete types sharing 10+ identical fields. No FEC-specific subtypes — ticket explicitly says those belong in enterprise B-2. `Obligation.apply_event()` is the one behavior method, directly driven by the acceptance criterion.
- writing-code:3 (verify symbols exist): `SourceAwareModel`, `generate_entity_uuid4` imported from `socialwarehouse.core.mixins` — verified. Removed `IdentifiableModel` since transactions are artifacts (UUID4), not identity entities (UUID5). All 6 models exported from `socialwarehouse.transactions.models.__init__`. M2M relations on `TransactionGroup` use the correct `to=` model string.
- writing-code:7 (no silent processes): `Contribution.save()` sets `transaction_type = "contribution"` explicitly — no silent type inference. `Obligation.apply_event()` updates balance and status with explicit conditional logic. Balance floored at 0.00 to prevent negative balances.

### writing-tests:1, writing-tests:2
- writing-tests:1 (tests fail on revert): `test_full_repayment_marks_paid` asserts `status == "paid"` after balance hits 0 — removing `apply_event` logic breaks it. `test_jfc_distribution_multi_leg` creates 1 contribution + 3 transfers in a group — removing M2M fields breaks it. `test_query_contributions_by_state_and_date` filters by jurisdiction_state + year — removing those fields breaks the query.
- writing-tests:2 (tests import module under test): All test classes import from `socialwarehouse.transactions.models`.

### writing-claims:1, writing-claims:2
- writing-claims:1 (grep before declaring complete): 6 models verified via sqlite. Migration has 6 CreateModel + 14 AddIndex = 20 operations. 3 Delta schemas (transactions, obligations, transaction_groups) registered in TABLES dict.
- writing-claims:2 (countable claims grounded): 20 migration operations. 22 test methods across 7 test classes. All verified.

### writing-prose:1
- writing-prose:1 (no AI-typographic unicode): No smart quotes or special characters.

## Lead review (approach-fit, blast radius, sequencing)

As data engineer: Transactions partitioned by (jurisdiction_level, jurisdiction_state, year) for the primary analytical access pattern ("all TX contributions in 2024"). Merge key is (data_source, source_record_id) for dedup. Obligations SCD2 in Delta for balance history. `_TransactionBase` inherits `SourceAwareModel` only (not `IdentifiableModel`) — transactions are UUID4 artifacts, not UUID5 identity entities.

As tech lead: blast radius is additive-only. New `socialwarehouse.transactions` app. No existing models modified. INSTALLED_APPS updated. No FEC-specific subtypes — clean general-purpose civic ontology.

## Evidence-predates-work
Artifact: .self-review-sw290-transaction-types.md
First-added commit: artifact included in the same commit
Work commit: (determined at commit time)
