## Assumptions

Working as: software engineer (demographic domain asset author, SW#278 implementation).
Peer review needed from: tech lead (operator, dheerajchand, SW maintainer).
Lead review needed from: tech lead — merge gate.
Goal source: SW#278 https://github.com/siege-analytics/socialwarehouse/issues/278 (sub-issue of SW#275).
Plan reference: design note in session 260531-sharp-opal/plans/sw278-design-note.md.

## Peer review (mechanics, correctness, craft floor)

**writing-claims:1 (grep before declaring complete).** Verified each promised deliverable:

1. `demographic.py` asset module: `grep -n "all_assets" socialwarehouse/orchestration/assets/demographic.py` → `all_assets` list with 5 entries. Bronze SourceAsset + 2 delta_table_assets (silver, gold) + 2 postgis_materialization_assets (ACS, Decennial). PASS.
2. `__init__.py` registration: `grep "demographic" socialwarehouse/orchestration/assets/__init__.py` → imported + exported in `__all__`. PASS.
3. `definitions.py` wiring: `grep "demographic" socialwarehouse/orchestration/definitions.py` → imported, assets spread into Definitions. PASS.
4. Topology tests: `wc -l tests/orchestration/test_demographic_assets.py` → 78 lines, 11 test methods covering keys, deps, and registration. PASS.

**writing-code:7 (no untyped placeholder values).** The `_compute_demographics_typed` and `_compute_demographics_enriched` functions have placeholder bodies (`typed_df = bronze_df`, `enriched_df = silver_df`). This follows the same pattern as geo.py's `_compute_addresses_typed` — the orchestration layer wires the asset graph; the transform body is a follow-on concern. Acknowledged explicitly in code comments and docstrings.

**writing-code:15 (every blocking I/O call declares a timeout).** No new I/O calls introduced — the factories handle Spark session + PostGIS connection via the existing resources, which already declare timeouts (PostGISResource: 300s statement_timeout, SparkResource: delegates to Spark config). PASS.

**Pattern fidelity.** The demographic module follows the geo.py pattern exactly:
- SourceAsset for bronze (external ingest)
- `delta_table_asset()` for silver + gold
- `postgis_materialization_asset()` for PostGIS targets
- `all_assets` list at bottom
- Lazy imports inside compute functions
- `context.log.info` for row counts

**App label verification.** `grep label socialwarehouse/warehouse/apps.py` → `sw_warehouse`. Used in `target_django_app_label="sw_warehouse"` for both PostGIS assets. PASS.

**Delta schema alignment.** `SILVER_DEMOGRAPHICS` in `delta/tables.py` has 7 fields: geoid, vintage_year, summary_level, variable_code, survey_type, estimate, margin_of_error. The gold layer's two PostGIS targets filter on `survey_type` (acs vs decennial) — this column exists in the schema. PASS.

**No accidental file moves, no debug prints, no AI attribution.** Diff inspection confirms clean additions only.

## Lead review (approach-fit, blast radius, sequencing)

Approach-fit: follows the factory-driven pattern established in geo.py and documented in `docs/orchestration/how-to-add-asset-to-existing-domain.md`. Two PostGIS targets from one gold source is the correct topology — FactACSEstimate and FactDecennialCount have different schemas (estimate+MOE vs count).

Wraps-not-reinvents: compute functions reference `socialwarehouse.delta.config.get_table_path()` for paths, `socialwarehouse.warehouse.services.census_loader` for variable metadata (via comments indicating delegation points). No reimplementation of Spark session creation or Census API logic.

Blast radius: 1 new asset module + 1 new test file + 2 lines changed in each of `__init__.py` and `definitions.py`. No existing behavior modified. The `[orchestration]` extra is still opt-in.

Sequencing: develop-first merge. The civic domain (SW#277) is a sibling PR; both can merge independently since they touch separate domain modules and both add to the assets list.

Approach-fit verdict: PASS.
