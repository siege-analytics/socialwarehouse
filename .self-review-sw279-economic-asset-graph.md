## Self-Review: economic domain asset graph (SW#279)

### Role declarations
- Author: AI agent
- Reviewer: AI agent (self-review)

Working as: software engineer

## Assumptions

Goal source: SW#279 — feat: add economic domain Dagster asset graph. Two parallel medallion pipelines (BLS QCEW and IRS SOI) following the factory-driven pattern established by geo.py.

- Two independent bronze->silver->gold pipelines (QCEW and SOI) — no cross-dependency between them
- Three PostGIS materialization targets: BLSQCEWAggregate, IRSSOIAggregate, IRSSOIIncomeBucket
- Django app label is `sw_economic` (verified from apps.py)
- Compute functions are placeholder pass-throughs (same pattern as geo.py and demographic.py); real transforms will be filled in when the delta enrichment layer stabilizes
- IRSSOIIncomeBucket materializes from the same gold.soi_enriched table as IRSSOIAggregate, extracting distinct bucket dimension rows

## Peer review

writing-code:1 — asset module follows the exact factory pattern from geo.py and demographic.py. SourceAsset for bronze, delta_table_asset for silver/gold, postgis_materialization_asset for PostGIS. No new abstractions introduced.

writing-code:2 — definitions.py and __init__.py updated to register the economic module. Asset list spread uses the same [*module.all_assets] pattern.

writing-claims:1 — app_label `sw_economic` verified via grep of apps.py. Model names match the three models exported from economic/models/__init__.py.

## Lead review

- [x] `economic.py` follows geo.py/demographic.py factory pattern exactly
- [x] Two independent pipelines (QCEW, SOI) — correct separation of concerns
- [x] All three Django models have PostGIS materialization assets
- [x] `__init__.py` exports the economic module
- [x] `definitions.py` includes economic.all_assets in the Definitions object
- [x] Asset keys follow the `warehouse/<layer>/<table>` convention
- [x] No new dependencies introduced; uses existing factory functions only
- [x] No AI attribution in any file
