# Core dependencies for the Social Warehouse containers.
# The socialwarehouse + swh packages are installed via `pip install -e .` at runtime
# (since the repo is volume-mounted at /opt/social_warehouse/).

# siege-utilities pinned to a hotfix on the v3.23.0 release point: v3.23.0 +
# the siege_geo migration-0006 fix (0006 re-added RedistrictingPlan.state that
# 0005 already adds, crashing a fresh `migrate` with DuplicateColumn). The
# unpinned >=3.17.2 floated to 3.23.0 and broke CI. Revert to a released
# version constraint once siege_utilities cuts a release containing the fix.
siege-utilities[geo] @ git+https://github.com/siege-analytics/siege_utilities@d77c54c8af49b7fe2e6448bbc455fa0d5cc5fa97
# Cap below Django 6: the warehouse is authored for Django 5.2; an unpinned
# >=5.2 floated to 6.0.x. Lift the cap once a 6.x migration is done deliberately.
Django>=5.2,<6
djangorestframework>=3.16
djangorestframework-gis>=1.0
django-grappelli>=4.0
django-postgres-copy>=2.7
celery[redis]>=5.6
click>=8.1
geopandas>=1.1
pandas>=2.2
pydantic>=2.10
pydantic-settings>=2.0
psycopg2-binary>=2.9
sqlalchemy>=2.0
pyproj>=3.7
shapely>=2.1
geopy>=2.4
neo4j>=5.0
ipython
ipdb
